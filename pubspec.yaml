name: recipe_finder
description: Find a recipe for your delicious meal.

publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ">=2.7.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  http: "0.13.1"
  flutter_dotenv: "5.0.2"
  cupertino_icons: "1.0.2"

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: "1.0.0"

flutter:
  uses-material-design: true
  assets:
    - .env